###############################################################################
# Service account the workload runs as.
#
# No IRSA annotation: credentials arrive through an EKS Pod Identity
# association created against this namespace and name. See the README for the
# association command, and for the legacy IRSA alternative.
###############################################################################
apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudgeeks-secrets
  namespace: app
